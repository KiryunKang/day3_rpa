# 민원 챗봇(OpenAI)용 키. 아래 값을 채운 뒤 파일명을 .env 로 복사하세요.
# 발급: https://platform.openai.com/api-keys
# 주의: .env 는 git에 커밋되지 않습니다(.gitignore 처리됨).
OPENAI_API_KEY=sk-여기에_키_입력
# 선택: 사용할 모델 (기본값 gpt-4o-mini)
# OPENAI_MODEL=gpt-4o
