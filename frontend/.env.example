# 프론트엔드 환경변수 템플릿 (Vite). 실제 값은 .env.local 또는 CI Variables로 주입.

# 배포된 백엔드 API 주소. 개발(프록시) 환경에서는 비워두면 상대경로(/api)로 동작.
# 예) VITE_API_BASE=https://day3-rpa-api.onrender.com
VITE_API_BASE=

# (선택) GitHub Pages base 경로 오버라이드. 기본값 /day3_rpa/.
# 커스텀 도메인/유저페이지면 VITE_BASE=/ 로 설정.
# VITE_BASE=/day3_rpa/
